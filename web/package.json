{
  "version": "0.1.0",
  "name": "electric-sql-web-demo",
  "scripts": {
    "build": "esbuild src.ts worker.js --bundle --external:fs --format=esm --splitting --outdir=dist",
    "serve": "npx lite-server"
  },
  "browser": {
    "path": "path-browserify"
  },
  "dependencies": {
    "@aphro/absurd-sql": "^0.0.53",
    "@aphro/sql.js": "^1.7.0",
    "electric-sql": "^0.1.3",
    "fastestsmallesttextencoderdecoder": "^1.0.22",
    "lodash.throttle": "^4.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.15.10",
    "lite-server": "^2.6.1",
    "path-browserify": "^1.0.1"
  }
}
